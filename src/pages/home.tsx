import HeroSection from '@/components/HeroSection';

function Home() {
    return (
        <section id="home" className="min-h-screen">
            <HeroSection />
        </section>
    );
}

export default Home;
